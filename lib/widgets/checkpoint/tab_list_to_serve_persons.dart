import 'package:flutter/material.dart';

class TabListToServePersons extends StatelessWidget {
const TabListToServePersons({ super.key });
  @override
  Widget build(BuildContext context){
    return Text("List to serve");
  }
}