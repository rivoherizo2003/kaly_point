class SessionPerson {
  int id;
  int personId;
  int sessionId;
  DateTime createdAt;
  SessionPerson({
    required this.id,
    required this.personId,
    required this.sessionId,
    required this.createdAt,
  });
}
